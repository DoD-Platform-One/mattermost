istio:
  enabled: true

global:
  imagePullSecrets:
    - name: private-registry-mil

minio:
  install: true
  imagePullSecrets:
    - name: private-registry-mil

postgresql:
  install: true
  image:
    pullSecrets:
      - private-registry-mil
